---
import { Icon } from 'astro-icon/components';
import { t } from '../i18n/index';
import { type Locale, localeNames, localeFlags, localizedPath, getAlternatePath } from '../i18n/config';

interface Props {
  currentPath?: string;
  lang?: Locale;
}

const { currentPath = '/', lang = 'en' } = Astro.props;
const i = t(lang);
const prefix = lang === 'fr' ? '/fr' : '';

const navLinks = [
  { href: `${prefix}/`, label: i.nav.home },
  { href: `${prefix}/services`, label: i.nav.services },
  { href: `${prefix}/products`, label: i.nav.products },
  { href: `${prefix}/about`, label: i.nav.about },
  { href: `${prefix}/gallery`, label: i.nav.gallery },
  { href: `${prefix}/contact`, label: i.nav.contact },
];

const alt = getAlternatePath(currentPath);
---

<header class="fixed top-0 left-0 right-0 z-50 transition-all duration-300" id="main-header">
  <!-- Announcement Bar -->
  <div class="bg-red text-white py-2 text-center text-sm font-medium">
    <div class="container">
      <p>
        {i.nav.announcement}
        <a href={`${prefix}/book`} class="underline ml-2 hover:no-underline font-semibold">{i.nav.reserveNow}</a>
      </p>
    </div>
  </div>
  
  <!-- Main Navigation -->
  <nav class="bg-white border-b border-mist py-4" aria-label="Main navigation">
    <div class="container">
      <div class="flex items-center justify-between">
        <!-- Logo -->
        <a href={`${prefix}/`} class="flex items-center gap-3 group" aria-label="Ultimate Beauty Spa - Home">
          <img src="/images/logo.svg" alt="Ultimate Beauty Spa" class="h-12 w-auto group-hover:scale-105 transition-transform duration-300" />
          <div class="flex flex-col">
            <span class="text-xl font-bold text-black tracking-tight">Ultimate Beauty</span>
            <span class="text-xs uppercase tracking-[0.2em] text-red font-semibold -mt-0.5">Spa</span>
          </div>
        </a>
        
        <!-- Desktop Navigation -->
        <div class="hidden lg:flex items-center gap-6">
          <ul class="flex items-center gap-7" role="list">
            {navLinks.map(link => (
              <li>
                <a 
                  href={link.href}
                  class={`text-sm uppercase tracking-wider font-semibold transition-colors duration-300 link-underline ${
                    currentPath === link.href 
                      ? 'text-red' 
                      : 'text-charcoal hover:text-red'
                  }`}
                  aria-current={currentPath === link.href ? 'page' : undefined}
                >
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
          
          <div class="flex items-center gap-3">
            <a href="tel:+237697113339" class="flex items-center gap-2 text-charcoal hover:text-red transition-colors duration-300" aria-label={i.nav.callUs}>
              <Icon name="lucide:phone" class="w-5 h-5" />
              <span class="text-sm font-semibold hidden xl:inline">697 113 339</span>
            </a>
            <a href="#booking-modal" data-booking-modal class="btn-primary">
              <span>{i.nav.bookNow}</span>
              <Icon name="lucide:arrow-up-right" class="w-4 h-4" />
            </a>
            <!-- Language Pill Toggle (Desktop) -->
            <div class="lang-pill" role="radiogroup" aria-label="Language">
              <a 
                href={lang === 'en' ? currentPath : alt.path}
                class={`lang-pill__opt ${lang === 'en' ? 'lang-pill__opt--active' : ''}`}
                role="radio"
                aria-checked={lang === 'en' ? 'true' : 'false'}
              >
                EN
              </a>
              <a 
                href={lang === 'fr' ? currentPath : alt.path}
                class={`lang-pill__opt ${lang === 'fr' ? 'lang-pill__opt--active' : ''}`}
                role="radio"
                aria-checked={lang === 'fr' ? 'true' : 'false'}
              >
                FR
              </a>
              <div class={`lang-pill__slider ${lang === 'fr' ? 'lang-pill__slider--fr' : ''}`}></div>
            </div>
          </div>
        </div>
        
        <!-- Mobile Menu Button -->
        <button 
          class="lg:hidden p-2 text-charcoal hover:text-red transition-colors"
          aria-label={i.nav.openMenu}
          aria-expanded="false"
          aria-controls="mobile-menu"
          id="menu-toggle"
        >
          <Icon name="lucide:menu" class="w-6 h-6" />
        </button>
      </div>
    </div>
  </nav>
  
  <!-- Mobile Menu -->
  <div class="mobile-menu lg:hidden" id="mobile-menu" aria-hidden="true">
    <div class="h-full flex flex-col">
      <!-- Mobile Menu Header -->
      <div class="flex items-center justify-between p-6 border-b border-mist">
        <a href={`${prefix}/`} class="flex items-center gap-3">
          <img src="/images/logo.svg" alt="Ultimate Beauty Spa" class="h-10 w-auto" />
          <span class="text-xl font-bold text-black">Ultimate Beauty</span>
        </a>
        <button 
          class="p-2 text-charcoal hover:text-red transition-colors"
          aria-label={i.nav.closeMenu}
          id="menu-close"
        >
          <Icon name="lucide:x" class="w-6 h-6" />
        </button>
      </div>
      
      <!-- Mobile Menu Links -->
      <nav class="flex-1 flex flex-col justify-center px-6" aria-label="Mobile navigation">
        <ul class="space-y-6" role="list">
          {navLinks.map((link, index) => (
            <li style={`animation-delay: ${index * 100}ms`}>
              <a 
                href={link.href}
                class={`text-3xl font-bold transition-colors duration-300 ${
                  currentPath === link.href 
                    ? 'text-red' 
                    : 'text-black hover:text-red'
                }`}
                aria-current={currentPath === link.href ? 'page' : undefined}
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
        
        <!-- Mobile Language Pill -->
        <div class="mt-8 flex justify-center">
          <div class="lang-pill lang-pill--lg" role="radiogroup" aria-label="Language">
            <a 
              href={lang === 'en' ? currentPath : alt.path}
              class={`lang-pill__opt ${lang === 'en' ? 'lang-pill__opt--active' : ''}`}
              role="radio"
              aria-checked={lang === 'en' ? 'true' : 'false'}
            >
              {localeFlags.en} English
            </a>
            <a 
              href={lang === 'fr' ? currentPath : alt.path}
              class={`lang-pill__opt ${lang === 'fr' ? 'lang-pill__opt--active' : ''}`}
              role="radio"
              aria-checked={lang === 'fr' ? 'true' : 'false'}
            >
              {localeFlags.fr} Français
            </a>
            <div class={`lang-pill__slider ${lang === 'fr' ? 'lang-pill__slider--fr' : ''}`}></div>
          </div>
        </div>
      </nav>
      
      <!-- Mobile Menu Footer -->
      <div class="p-6 border-t border-mist space-y-4">
        <a href="#booking-modal" data-booking-modal class="btn-primary w-full justify-center">
          <span>{i.nav.bookAppointment}</span>
        </a>
        <a href="tel:+237697113339" class="btn-secondary w-full justify-center">
          <Icon name="lucide:phone" class="w-4 h-4" />
          <span>+237 697 113 339</span>
        </a>
      </div>
    </div>
  </div>
</header>

<!-- Spacer for fixed header -->
<div class="h-[108px] lg:h-[108px]"></div>

<script>
  // Header scroll behavior
  const header = document.getElementById('main-header');
  let lastScroll = 0;
  
  window.addEventListener('scroll', () => {
    const currentScroll = window.pageYOffset;
    
    if (currentScroll > 100) {
      header?.classList.add('header-scrolled');
    } else {
      header?.classList.remove('header-scrolled');
    }
    
    lastScroll = currentScroll;
  });
  
  // Mobile menu toggle
  const menuToggle = document.getElementById('menu-toggle');
  const menuClose = document.getElementById('menu-close');
  const mobileMenu = document.getElementById('mobile-menu');
  
  function openMenu() {
    mobileMenu?.classList.add('is-open');
    mobileMenu?.setAttribute('aria-hidden', 'false');
    menuToggle?.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }
  
  function closeMenu() {
    mobileMenu?.classList.remove('is-open');
    mobileMenu?.setAttribute('aria-hidden', 'true');
    menuToggle?.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }
  
  menuToggle?.addEventListener('click', openMenu);
  menuClose?.addEventListener('click', closeMenu);
  
  // Close menu on escape key
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && mobileMenu?.classList.contains('is-open')) {
      closeMenu();
    }
  });
  
  // Close menu on link click
  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', closeMenu);
  });
  
  // Language pill toggle — no JS needed, it's just links
  // But we add a subtle haptic-like press effect
  document.querySelectorAll('.lang-pill__opt').forEach(opt => {
    opt.addEventListener('mousedown', () => {
      opt.classList.add('lang-pill__opt--press');
    });
    opt.addEventListener('mouseup', () => {
      opt.classList.remove('lang-pill__opt--press');
    });
    opt.addEventListener('mouseleave', () => {
      opt.classList.remove('lang-pill__opt--press');
    });
  });
</script>

<style>
  .header-scrolled nav {
    padding-top: 0.5rem;
    padding-bottom: 0.5rem;
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
  }
  
  /* ─── Language Pill Toggle ─── */
  .lang-pill {
    position: relative;
    display: inline-flex;
    align-items: center;
    background: var(--color-snow);
    border: 1.5px solid var(--color-mist);
    border-radius: 100px;
    padding: 0.1875rem;
    gap: 0;
  }

  .lang-pill__opt {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.375rem;
    padding: 0.375rem 0.875rem;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    color: var(--color-gray);
    text-decoration: none;
    border-radius: 100px;
    transition: color 0.3s var(--ease-out-expo);
    cursor: pointer;
    user-select: none;
    white-space: nowrap;
  }

  .lang-pill__opt:hover:not(.lang-pill__opt--active) {
    color: var(--color-charcoal);
  }

  .lang-pill__opt--active {
    color: white;
  }

  .lang-pill__opt--press {
    transform: scale(0.96);
  }

  .lang-pill__slider {
    position: absolute;
    top: 0.1875rem;
    left: 0.1875rem;
    width: calc(50% - 0.1875rem);
    height: calc(100% - 0.375rem);
    background: var(--color-red);
    border-radius: 100px;
    z-index: 1;
    transition: transform 0.35s var(--ease-out-expo);
    box-shadow: 0 2px 8px rgba(225, 29, 72, 0.3);
  }

  .lang-pill__slider--fr {
    transform: translateX(100%);
  }

  /* ── Mobile Pill (larger) ── */
  .lang-pill--lg {
    padding: 0.25rem;
  }

  .lang-pill--lg .lang-pill__opt {
    padding: 0.625rem 1.5rem;
    font-size: 0.9375rem;
    gap: 0.5rem;
  }
</style>
